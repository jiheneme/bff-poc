ktor {
  deployment {
    port = 8080
    port = ${?PORT}
  }
  application {
    modules = [
      com.***REMOVED***.SerializationKt.configureSerialization
      com.***REMOVED***.RoutingKt.configureRouting
    ]
  }
}